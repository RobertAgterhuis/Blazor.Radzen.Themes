@page "/catalog/data-filter-property"
@inject NavigationManager NavigationManager

<PageTitle>DataFilterProperty (Radzen)</PageTitle>

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/catalog/data-filter#data-filter-property", true);
    }
}
