@page "/catalog/body"
@inject NavigationManager NavigationManager

<PageTitle>Body (Radzen)</PageTitle>

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/catalog/layout-component#body", true);
    }
}
