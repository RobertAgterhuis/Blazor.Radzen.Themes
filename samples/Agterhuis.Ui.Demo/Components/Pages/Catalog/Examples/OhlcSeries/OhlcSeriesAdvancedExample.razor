<RadzenChart Style="height: 18rem;">
    <RadzenOhlcSeries Data="Rows" CategoryProperty="Date" OpenProperty="Open" HighProperty="High" LowProperty="Low" CloseProperty="Close" Title="AAPL" />
    <RadzenCategoryAxis FormatString="{0:MM/dd}" />
</RadzenChart>

@code {
    private static readonly IReadOnlyList<OhlcRow> Rows =
    [
        new(new DateTime(2026, 1, 1), 10, 14, 8, 12),
        new(new DateTime(2026, 1, 2), 12, 16, 11, 15),
        new(new DateTime(2026, 1, 3), 15, 18, 13, 17)
    ];

    private sealed record OhlcRow(DateTime Date, double Open, double High, double Low, double Close);
}
