<RadzenSpiderChart GridShape="SpiderChartGridShape.Polygon" ShowMarkers="true" ShowTooltip="true">
    <RadzenSpiderColumnSeries Data="Rows" CategoryProperty="Metric" ValueProperty="Value" Title="KPI" />
</RadzenSpiderChart>

@code {
    private static readonly IReadOnlyList<SpiderRow> Rows =
    [
        new("Snelheid", 5),
        new("Kwaliteit", 8),
        new("Samenwerking", 7),
        new("Stabiliteit", 6)
    ];

    private sealed record SpiderRow(string Metric, double Value);
}
