@typeparam TItem

<div class="agt-field-wrapper @CssClass"
     role="group"
     aria-describedby="@ResolvedDescribedBy"
     aria-invalid="@(!string.IsNullOrWhiteSpace(ValidationMessage))">
    @if (!string.IsNullOrWhiteSpace(Label))
    {
        <RadzenLabel Text="@Label" Component="@ResolvedName" class="agt-field-wrapper__label" />
    }
    else if (!string.IsNullOrWhiteSpace(AriaLabel))
    {
        <label class="agt-sr-only" for="@ResolvedName">@AriaLabel</label>
    }

    <RadzenAutoComplete Data="@Data"
                        Name="@ResolvedName"
                        Value="@Value"
                        ValueChanged="@ValueChanged"
                        ValueExpression="@ValueExpression"
                        Placeholder="@Placeholder"
                        Disabled="@Disabled"
                        TextProperty="@TextProperty"
                        FilterDelay="@FilterDelay"
                        MinLength="@MinLength"
                        AriaLabel="@AriaLabel"
                        LoadData="@LoadData"
                        class="agt-auto-complete"
                        Style="@Style">
        <EmptyTemplate>
            <span class="agt-auto-complete__empty">@EmptyText</span>
        </EmptyTemplate>
    </RadzenAutoComplete>

    @if (!string.IsNullOrWhiteSpace(ValidationMessage))
    {
        <p id="@MessageId" class="agt-field-wrapper__message" role="status" aria-live="polite">@ValidationMessage</p>
    }
</div>

@code {
    [Parameter]
    public IEnumerable<TItem>? Data { get; set; }

    [Parameter]
    public string? Value { get; set; }

    [Parameter]
    public EventCallback<string?> ValueChanged { get; set; }

    [Parameter]
    public Expression<Func<string?>>? ValueExpression { get; set; }

    [Parameter]
    public string? Name { get; set; }

    [Parameter]
    public string? Label { get; set; }

    [Parameter]
    public string? AriaLabel { get; set; }

    [Parameter]
    public string? Placeholder { get; set; }

    [Parameter]
    public string? TextProperty { get; set; }

    [Parameter]
    public bool Disabled { get; set; }

    [Parameter]
    public int FilterDelay { get; set; } = 300;

    [Parameter]
    public int MinLength { get; set; } = 1;

    [Parameter]
    public string EmptyText { get; set; } = "Geen resultaten";

    [Parameter]
    public EventCallback<LoadDataArgs> LoadData { get; set; }

    [Parameter]
    public string? CssClass { get; set; }

    [Parameter]
    public string? ValidationMessage { get; set; }

    [Parameter]
    public string? Style { get; set; }

    private readonly string _fallbackName = $"agt-autocomplete-{Guid.NewGuid():N}";
    private readonly string _messageId = $"agt-autocomplete-msg-{Guid.NewGuid():N}";

    private string ResolvedName => string.IsNullOrWhiteSpace(Name) ? _fallbackName : Name;

    private string MessageId => _messageId;

    private string? ResolvedDescribedBy => string.IsNullOrWhiteSpace(ValidationMessage) ? null : MessageId;

    protected override void OnParametersSet()
    {
        System.Diagnostics.Debug.Assert(
            !string.IsNullOrWhiteSpace(Label) || !string.IsNullOrWhiteSpace(AriaLabel),
            "AgtAutoComplete requires Label or AriaLabel for accessibility.");
    }
}
